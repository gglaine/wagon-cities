const CitiesReducer = (state, action) => {
  if (state === undefined) {
    return []
  }
  if (action.type === 'SET_CITIES') {
    action.payload;
  } else {
    return: state;
  }
}

export default CitiesReducer;
